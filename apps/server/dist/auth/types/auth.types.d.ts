import { SocialProviderTypes } from '../schema/authProvider.schema';
export declare const AuthTypes: {
    JWT: string;
    FACEBOOK: SocialProviderTypes.FACEBOOK;
    GOOGLE: SocialProviderTypes.GOOGLE;
};
